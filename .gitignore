# Variables de entorno — NUNCA subir a Git
.env
.env.local
.env.*.local
.env.production
.env.staging

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Herramientas locales de AI (no son parte del proyecto)
.agents/
.continue/
skills-lock.json
.claude/worktrees/

# Datos sensibles del onboarding del equipo (generados por scripts/create_team_users.mjs)
team_users.json
team_credentials.md
team_credentials.txt

# Backups temporales generados por scripts (.bak de leads.json, etc)
*.bak

# PII de clientes: lotes de alta masiva y volcados de respaldo (el repo es público)
supabase/batch_*.json
supabase/respaldo_*/

# Vistas previas de correo generadas por supabase/email_campana.mjs
preview-emails/
