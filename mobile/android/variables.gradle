ext {
    // La usa nuestro servicio de llamadas (app/build.gradle) y tiene que
    // coincidir con la que trae el complemento de avisos: dos versiones
    // distintas de la misma libreria hacen que el APK falle al armarse.
    firebaseMessagingVersion = '25.0.1'
    minSdkVersion = 24
    compileSdkVersion = 36
    // targetSdk 34 A PROPÓSITO (10-jul-2026): con target 35/36 Android fuerza
    // edge-to-edge → la app dibuja BAJO la barra de estado y en varios WebView
    // los safe-area insets llegan en 0 → "la parte de arriba sale cortada"
    // (reporte de Ángel, celular real). Con 34 la ventana queda DEBAJO de la
    // barra en todos los Android: determinista, sin depender de insets.
    // ⚠️ Google Play exige target 36 desde el 31-ago-2026. El build de Play
    // (android-play.yml pasa -PplayStore) compila con 36 y Capacitor ajusta
    // los márgenes del WebView de forma nativa (adjustMarginsForEdgeToEdge
    // en capacitor.config.json) — se valida con dispositivos reales en la
    // prueba cerrada de Play antes de tocar producción.
    targetSdkVersion = hasProperty('playStore') ? 36 : 34
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}